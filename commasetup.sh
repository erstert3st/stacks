setup 
# UPDATE
sudo apt update && sudo apt -y full-upgrade && sudo apt -y clean && sudo apt -y autoremove
# Check 32/64 bit should be arch64
uname -m
#
sudo apt install python3-full
#docker 
its updating so:
https://docs.docker.com/engine/install/debian/

#list hhd
sudo blkid

